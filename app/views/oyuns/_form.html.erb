<%= form_with(model: oyun, local: true) do |form| %>
  <% if oyun.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(oyun.errors.count, "error") %> prohibited this oyun from being saved:</h2>

      <ul>
      <% oyun.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :isim %>
    <%= form.text_field :isim %>
  </div>

  <div class="field">
    <%= form.label :aciklama %>
    <%= form.text_area :aciklama %>
  </div>
<div class="field">
       <%= form.label :resim %>
       <%= form.file_field :resim %>
</div> 
 
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
